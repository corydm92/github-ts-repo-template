# github-ts-repo-template

My base template for github repositories.

CI runs on pull requests and pushes to main to enforce formatting, linting, type-checks, and tests.

CD templates are available for npm publishing and Docker image builds via manual workflow dispatch.
