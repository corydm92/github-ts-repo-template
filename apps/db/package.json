{
  "name": "@app/db",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format:check": "pnpm -C ../.. exec prettier apps/db --check",
    "lint": "pnpm -C ../.. exec eslint apps/db --max-warnings=0 --no-error-on-unmatched-pattern",
    "type-check": "echo \"No TS checks for SQL-first db\"",
    "test": "echo \"No test specified\" && exit 0"
  }
}
