{
  "name": "@app/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format:write": "prettier . --write",
    "format:check": "prettier . --check",
    "test": "echo \"No test specified\" && exit 0",
    "ci": "pnpm run format:check && pnpm run test"
  }
}
