{
  "name": "@app/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format": "prettier . --write",
    "test": "echo \"No test specified\" && exit 0",
    "ci": "pnpm run format && pnpm run test"
  }
}
