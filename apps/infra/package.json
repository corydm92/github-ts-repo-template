{
  "name": "@app/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format:check": "pnpm -C ../.. exec prettier apps/infra --check",
    "lint": "pnpm -C ../.. exec eslint apps/infra --max-warnings=0 --no-error-on-unmatched-pattern",
    "type-check": "echo \"No TS checks for Terraform-first infra\"",
    "test": "echo \"No test specified\" && exit 0"
  }
}
