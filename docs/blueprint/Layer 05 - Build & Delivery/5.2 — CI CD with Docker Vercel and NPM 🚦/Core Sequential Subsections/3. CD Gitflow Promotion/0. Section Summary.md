# 0. Section Summary

CD rebuilds per environment using trunk-based triggers and deterministic inputs.

Triggers:
- main → dev deploy (if configured)
- v* tag → production deploy

Rule: rebuild per environment with deterministic inputs.
