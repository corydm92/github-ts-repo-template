# 0. Section Summary

CD rebuilds per environment using Gitflow triggers and deterministic inputs.

Triggers:
- develop → dev deploy
- release/* → staging deploy
- v* tag → production deploy

Rule: rebuild per environment with deterministic inputs.
