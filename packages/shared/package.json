{
  "name": "@pkg/shared",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format:write": "prettier . --write --ignore-path ../../.prettierignore",
    "format:check": "prettier . --check --ignore-path ../../.prettierignore",
    "lint": "echo \"No lint specified\" && exit 0",
    "type-check": "echo \"No type-check specified\" && exit 0",
    "test": "echo \"No test specified\" && exit 0",
    "build": "echo \"No build specified\" && exit 0",
    "ci": "pnpm run format:check && pnpm run lint && pnpm run type-check && pnpm run test && pnpm run build"
  }
}
